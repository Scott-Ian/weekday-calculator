import { Weekday } from './../src/weekday.js';

describe('Weekday', () => {
  let reusableDate;

  beforeEach(() => {
    reusableDate = new Weekday();
  });

  test('should correctly determine if a user has entered a date', () => {
    
  })
});